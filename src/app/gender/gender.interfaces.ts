export interface GenderReadOnlyDTO {
  genderId: number;
  genderDescription: string;
}

export interface GenderCreateDTO {
  genderDescription: string;
}

export interface GenderUpdateDTO {
  genderDescription: string;
}
